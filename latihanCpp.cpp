#include <iostream> // memanggil Library atau perpustakaan (wajib ada setiap program)
using namespace std; // menyederhanakan supaya gak perlu ketik std

int main(){ // pintu utama program
	int a; // int = integer (bilangan bulat), float = pecahan, char = karakter, dll
	cout << "Hai" << endl; // cout << output atau pengeluaran, endl = endline (akhir baris)
	cin >> a; // cin >> input atau pemasukan
	cout << a; // ; = titik koma menandakan penutup setiap baris program
}
